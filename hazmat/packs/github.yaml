pack:
  name: github
  version: 1
  description: GitHub CLI access via scoped token

session:
  env_passthrough: [GH_TOKEN]

warnings:
  - "GH_TOKEN gives the agent GitHub API access. Use a fine-grained PAT with minimal scopes."
