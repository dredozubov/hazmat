\* Model configuration with AgentContained invariant enabled.
\* This WILL find a violation — the security window between sudoers (step 8)
\* and pfAnchor (step 9).
\*
\* Run:
\*   java -jar ~/workspace/tla2tools.jar -workers auto -config MC_SetupRollback_WithContainment.cfg MC_SetupRollback.tla
\* Expected: AgentContained VIOLATED.

SPECIFICATION Spec

CONSTANTS
    MaxSetupAttempts    = 2
    MaxRollbackAttempts = 2

INVARIANTS
    TypeOK
    NoOrphanedArtifacts
    SudoersRequiresHelper
    AgentDepsRequireUser
    AgentContained
